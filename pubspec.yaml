name: app_ummel
description: Ummel Application

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter




  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  adobe_xd: ^2.0.0+1
  flutter_svg: ^0.21.0+1
  persistent_bottom_nav_bar: any
  image_picker: ^0.8.4
  image_cropper: ^1.0.2
  multi_image_picker2: ^5.0.2
  numberpicker: ^2.0.1
  flutter_staggered_grid_view: ^0.4.0
  image: ^3.0.8
  path_provider: ^2.0.7
  provider: ^6.0.1
  intl: ^0.17.0
  firebase_core: ^1.10.6
  firebase_in_app_messaging: ^0.6.0+5
  firebase_database: ^9.0.4
  firebase_auth: ^3.3.4
  cached_network_image: ^3.2.0
  flutter_cache_manager: ^3.3.0
  cloud_firestore: ^3.1.5
  firebase_storage: ^10.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - images/ak-selfie.jpg
    - images/dodge.jpg
    - images/huehnersuppe.jpg
    - images/katze.jpg
    - images/Melone.jpg
    - images/milchprodukte.jpg
    - images/oma-mit-waffe.jpg
    - images/stick.png
    - images/UmmelLogo.png
    - images/hausgarten.png
    - images/fahrzeug.png
    - images/baby.png
    - images/bauenbasteln.png
    - images/kleidung.png
    - images/kosmetik.png
    - images/lebensmittel.png
    - images/pfand.png
    - images/sportfreizeit.png
    - images/technik.png
    - images/tierbedarf.png
    - images/unterhaltung.png
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  fonts:
    - family: ummel_icons
      fonts:
        - asset: fonts/ummel_icons.ttf
    - family: ummel_cat_icons
      fonts:
        - asset: fonts/ummel_cat_icons.ttf
    - family: Paragraph
      fonts:
        - asset: fonts/Paragraph.ttf
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages